version: '3'
services:
  usermicroservices:
    build: ./BackEnd/usermicroservices
    ports:
     - "8080:8080"
    volumes:
     - .:/code
  bookmicroservices:
    build: ./BackEnd/bookmicroservices
    ports:
      - "8081:8081"
    volumes:
     - .:/code